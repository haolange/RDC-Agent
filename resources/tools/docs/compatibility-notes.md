# Compatibility Notes

The compatible public surface is CLI:

- `rdx.bat`
- `bin/rdx`
- `python cli/run_cli.py`

`rdx-tools` 1.x keeps the canonical JSON envelope stable. Use `rdx.bat version --json` to inspect the tool version, schema version, platform, entrypoints, and compatibility metadata.
