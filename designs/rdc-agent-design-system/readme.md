# RDC-Agent Design System Preview

只读预览，直接引用运行时真值：

- `src/renderer/styles/design-system.css`
- `src/renderer/ui/kit.css`（`.button-*` 与分子组件 class）

不要再复制 token 或 `styles.css`。改视觉只改 runtime CSS，然后刷新本页。

打开：`designs/rdc-agent-design-system/Design System Preview.html`。
