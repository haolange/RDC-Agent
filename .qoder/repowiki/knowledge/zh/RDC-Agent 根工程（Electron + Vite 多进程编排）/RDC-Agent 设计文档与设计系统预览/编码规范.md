- 文档按职责分目录组织（contracts/architecture/product/ui/workflows），并通过 `docs/README.md` 给出固定阅读顺序，引导读者从顶层契约逐步深入。
- 架构与实现类文档统一指向 `contracts/` 中的契约文件，避免同一主题存在多份权威说明。
- Design System 预览采用只读 HTML 形式，直接引用运行时 CSS 而非复制样式 token，保证预览与真实 UI 始终同步。