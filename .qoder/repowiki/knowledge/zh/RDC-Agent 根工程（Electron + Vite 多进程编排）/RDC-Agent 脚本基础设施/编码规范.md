- 所有检查类脚本以 check-*.mjs 命名并按单一职责拆分，便于在 CI 中独立启用或跳过。
- 保真度相关脚本依赖 scripts/fidelity/ 下的 JSON/TXT 基线文件进行 diff 比对，新增变更需先更新基线。
- 需要访问 TypeScript 源码的脚本统一通过 register-ts-source.cjs 注入 ts-node/esm 支持后再执行。
- 跨平台启动脚本成对提供 .sh 与 .cmd 版本，由上层命令根据 OS 选择对应实现。