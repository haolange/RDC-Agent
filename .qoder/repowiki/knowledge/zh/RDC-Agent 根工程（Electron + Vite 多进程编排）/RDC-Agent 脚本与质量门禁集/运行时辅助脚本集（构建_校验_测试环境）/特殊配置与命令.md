`register-ts-source.cjs` 需在任何需要直接 require TypeScript 源码的脚本中先行 `require('./register-ts-source.cjs')`（如 `builtin-profile-contracts.mjs`），以启用 `.ts/.tsx` 扩展与 `@shared/` 别名；`pnpm-resolver.mjs` 要求系统中存在精确版本 `11.7.0` 的 pnpm，否则抛出包含已探测候选列表的错误。