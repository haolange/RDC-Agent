纯浏览器端 HTML/CSS/JS 零依赖方案，使用 IIFE 暴露全局命名空间（`CotScenarios`、`CotMockRuntime`、`CotRender`、`CotI18n`、`CotShell`）进行模块间通信，通过 CSS class（如 `conversation-bubble`、`cot-shell`）和 data attribute（`data-scenario`、`data-play`）完成 UI 绑定。