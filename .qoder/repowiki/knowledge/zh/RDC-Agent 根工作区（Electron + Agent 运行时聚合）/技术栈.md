Electron 42 + electron-vite 5 + React 18 + TypeScript 5 + Vitest 4 + pnpm 11；AI 能力基于 `ai` SDK 与 `@ai-sdk/provider`，Markdown 渲染使用 react-markdown + rehype/rehype-katex + remark-gfm/math，代码高亮用 highlight.js，图表用 mermaid，PDF 预览用 pdfjs-dist，状态管理用 zustand，配置持久化用 electron-store 与 zod schema。