运行任意 `check-*.mjs` 需在仓库根目录执行且需已安装 pnpm 与 node_modules；系统债务检查可通过 `--emit-measured` 输出新基线、通过 `--base-ref <sha>` 或 `RDC_SYSTEM_DEBT_BASE_REF` 指定历史基进行单调性比较；Fidelity 检查前需先运行 `pnpm run fidelity:extract` 更新 `scripts/fidelity` 基线；Vitest 全局设置会自动创建 `rdc-agent-vitest-*` 临时目录并通过 `RDC_AGENT_*` 环境变量隔离用户数据。