覆盖率门禁需先执行 `pnpm run test:coverage` 生成 `coverage/coverage-summary.json`，缺失基线时可通过设置 `RDC_AGENT_COVERAGE_RATCHET_INIT=1` 自动写入 `scripts/fidelity/coverage-ratchet.json`；系统债务门禁通过 `--base-ref` 或 `RDC_SYSTEM_DEBT_BASE_REF` 指定历史比较基准，CI 中要求非零 SHA。