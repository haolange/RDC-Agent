# UI 交互审查与收敛

## 2026-09-22 Agent 列表视觉比例收敛

范围仅为设置中的 Agent 行；复用 ListRow / OverflowFade / ModeGlyph，以 feature 内 AgentListItem 集中组合。保留此前用户菜单和等高行为，不改变数据或权限，不提交或发布。此前等高证据仍适用于当时构建，不能替代本轮视觉比例验收。

| 工作项 | 依赖 | 状态 | 验收 |
| --- | --- | --- | --- |
| AgentListItem 与旧样式退役 | 已批准计划 | 通过 | ListRow 展示变量解决后加载 padding 覆盖；旧40px底座及重复文字布局退役，其他caller默认值不变 |
| 独立工程验证及文档 | 实现 | 通过 | 独立115文件474项renderer测试、定向3文件10项、tsc、全量lint、token/structure/appearance/fidelity/settings-agents/legacy与build通过 |
| 真实 Browser 与独立视觉复核 | 最新构建 | 通过 | bundle index-BWE38meJ.js；深色中文/浅色英文、三档字号、宽屏及390CSS px实测；四真实Agent与长短空样本同字号高度差0；独立源码与四图对照无finding |

实际比例：旧行约41px、40px底座且上下padding为0；本轮图形18px、透明图标列28px、上下padding各8px、行距4px。小/中/大字号行高约45.58/48.08/50.44–50.58px（浏览器缩放舍入差异），各组内部差0。名称和说明分别一行，空说明保留位置；短说明不渐隐，长说明实际溢出才渐隐。390px列表内部滚动可选择末项，无横向溢出；Tab焦点环及Enter选择不改变尺寸。保留共享ListRow已有hover/focus状态优先级，选中边界仍可辨，不添加Agent私有交互覆盖。disabled行为由独立组件测试验证，不冒充页面存在禁用Agent行。

收尾：独立Review实际读取原用户截图及本轮深色、浅色、窄屏三张真实截图，确认分隔感、图标权重及文字留白，无finding；这是独立截图复核，不冒充独立live交互。主线程完成真实点击与测量。关闭草稿后清理三个本轮项目样本并刷新，文件与UI均确认只剩原有UI Review；真实用户资源未修改。复用现有隔离QA，无新进程；canonical桌面锁不存在，桌面启动权已交还（锁检查）。保留最新中文深色大字号用户Agent页，console error/warn为空，无提交、推送或发布。

## 2026-09-21 Agent 列表与用户菜单增量修正

基于下方六图收敛后的两张新反馈；保留既有工作区，不含提交、推送、发布。上一轮组件复用检查不等于本轮行高、对齐与密度验收。

| 工作项 | 依赖 | 状态 | 验收 |
| --- | --- | --- | --- |
| 共享单行渐隐与 Tabs 满宽模式 | 已批准计划 | 通过 | 独立3文件20项定向测试；真实溢出双向变化、字体loadingdone/ready、卸载清理与默认自然宽度通过 |
| Agent 等高行与 Composer 渐隐迁移 | 共享组件 | 通过 | 19条长/短/空描述资源在390/640/960/1591px等高41.212px，列表自身滚动且无横溢出；Composer模型胶囊及菜单可用 |
| 用户菜单紧凑横向三行 | Tabs 满宽模式 | 通过 | 菜单352→约237px；三组边界差0、组内宽差<0.02px；中英/全部字号/深浅关键组合无溢出；键盘、外点、焦点返回、刷新保持通过 |
| 文档、独立工程验证及审查 | 实现 | 通过 | 独立114文件471项renderer测试、类型/全量lint/专项/build通过；另一独立Review无finding；两张真实截图独立视觉审查通过 |
| 真实 Browser 验收与环境收尾 | 最新构建 | 通过 | 最终bundle index-BAqOZqgG.js；英文大字号390px溢出复验关闭；临时18条资源清理，保留现有隔离QA与最新页面，canonical桌面锁不存在 |

本轮不改变 IPC、持久化和权限；不新增依赖。沿用隔离用户资源与当前小型 QA 项目，不改写真实用户配置。

收口日期：2026-09-22。英文大字号首次实测发现 `Simplified Chinese` 超出按钮约4px；统一既有语言选项为自称 `简体中文 / English` 后重新构建，390×844实测所有菜单tab溢出为0。追加字体加载完成/迟到Promise用例，未降低字号、点击区域或断言。独立工程检查包括全量renderer测试、tsc、全量ESLint、design-tokens、renderer-structure、appearance、fidelity、settings-agents、legacy-residue与production build。

视觉证据边界：主会话在真实同源Browser完成点击、连续缩放、语言/主题/字号变更、外点/Escape及刷新恢复；低高度429×242时菜单限高210px，内部滚动后Settings键盘Enter可达。独立审查者的浏览器会话无法枚举该标签，故独立视觉复核使用主会话保存的最终真实截图，不冒充其亲自交互。菜单与Agent截图保留为最小证据集；19条项目资源含18条本轮临时夹具，验后只删除这18条，原有UI Review及用户副本保留。复用原QA进程，没有新增launcher或修改真实桌面配置；桌面启动权已交还（canonical锁检查）。此前六图任务的Knowledge聚合门禁/原生IME边界仍按下方记录，不用本轮局部通过覆盖。

## 2026-09-21 控件与设置页收敛任务

范围：六张用户标注图及全 App 同类控件；不含提交、推送和发布。以下是本次工作状态，不覆盖下方历史证据。

| 工作项 | 依赖 | 状态 | 验收 |
| --- | --- | --- | --- |
| 共享 Button / Tabs / Textarea / Select / ListRow / EmptyState / 搜索多选 | 无 | 通过 | 独立工程与实际页面通过；普通输入实测32→165（八行上限）→32px；菜单主按钮中性实色与强调色描边，单选/焦点/禁用可辨 |
| 主进程技能选择投影 | 无 | 通过 | 独立复验3文件29测试通过；实现侧13文件277测试含 contracts；保留既有 parser/执行语义 |
| 常规、资源页和全 App 输入迁移 | 共享控件 | 通过 | 六图页面已实际复验；Skills 960/640/390px文档与保存可达、非法YAML保留草稿；新建未保存离开保护、取消、删除确认/Escape；MCP紧凑/Hooks填充。生产消息编辑聚焦/长短输入/清空禁用发送/取消/窄屏/缩放草稿保留通过 |
| Agent 技能选择接线 | 控件、技能投影 | 通过 | 5文件31测试及最终renderer复验；真实builtin/user/project候选、搜索/无结果、缺失ID保留、切目标不可用提示、自动保存与刷新顺序、390px可操作 |
| 文档与退役路径清理 | 实现 | 通过 | 权威文档、组件说明、门禁契约同步；AutosizeTextarea/DropdownSelect/ResourceEmptyState旧入口与死样式删除，fidelity通过 |
| 独立工程验证 | 实现与文档 | 阻塞 | 428文件3013项通过/4项既有跳过，coverage ratchet通过；最终renderer113文件464项、P2生命周期3文件16项、scoped/contracts12文件261项、type/lint/build及专项通过。仅最终聚合gates在Knowledge沙箱realpath EPERM停止；同任务未变main/shared的获批Knowledge门禁通过，不将最终聚合命令标绿 |
| 真实 Browser 视觉验收 | 最新构建 | 待验证 | 八Settings、Knowledge导入、Composer双模式、消息编辑、右栏五卡；深浅/中英/中大字号/960、640、390关键场景已通过。最终生产构建修改后Escape关闭约61ms，重开保留修改。仅原生IME组合仍受工具能力限制 |
| 独立审查与环境收尾 | 验证 | 通过 | Review的300ms关闭丢草稿P2已修复，独立Verify PASS后复审无剩余finding；废弃自有实例停止/冗余副本清理，保留最新可操作页面 |

确认方向：中性实色细边框按钮、强调色描边主动作、低饱和单选；普通文本一至八行自动伸缩；文档填满余高；技能可搜索多选；策略资源在上，全局阈值在下。用户原数据不改写，不复制大型 capture，不以隔离空态冒充真实账号/模型验收。

本轮证据边界：基于 `1acae3a2202075ff99d14bba8aa75b8eccd44dde` 的未提交工作区，不把基线SHA当成包含新实现的提交。Browser使用main-owned隔离用户资源副本与临时小型项目；技能/Agent为真实解析资源，消息编辑夹具显式标记为QA合成。没有复制secret、没有Provider请求、没有原生RDC执行；不覆盖历史全产品矩阵。原生中文IME组合事件在内置浏览器接口返回不支持，普通中文输入/换行与确定性composition测试分别记录。

本轮实测修复：项目配置加载先于currentProjectId导致的Settings草稿初始化白屏；窄屏Settings受桌面比例和资源最小高度限制导致的编辑区溢出；UserMenu私有实心主按钮覆盖；Hooks独立空态仍用compact；Knowledge重复导入动作。均沿原组件/契约修复，没有加入兼容fallback或削弱作用域校验。

环境清理：废弃QA实例已按父子进程/命令行归属停止；临时用户副本的旧配置备份、复制来的旧运行记录和空运行目录已清理，原始用户目录未动。按用户要求保留最终production Browser bridge、隔离数据与小型项目；页面停在用户技能详情，1806×1145、深色中文/大字号，编辑区619px、无拖拽/横向溢出，最终构建载入后未见新增console error/warn。canonical桌面锁不存在，桌面启动权已交还（锁检查）；保留QA使用自己的隔离锁，不宣称真实账号桌面启动通过。用户Agent 5文件、Skill 7文件与原始用户资源逐文件哈希一致；项目QA文件的编辑仅作用于临时项目。

剩余解阻条件：完整聚合门禁需在允许Knowledge测试访问本机用户路径的环境中复跑；本轮追加权限申请曾被安全审核拒绝，未绕过。原生IME组合需人工使用真实中文输入法确认；工程composition用例、普通中文输入/换行已通过，不代替原生组合提交。未执行Git提交、推送或发布。

以下2026-09-06记录为历史快照，其失败、未验证与清理状态不描述本轮工作区。

审查日期：2026-09-06。范围是所有用户可达入口的功能、布局、反馈、键盘操作与响应式。当前为**部分完成，未达到全产品验收完成条件**。遵守 DESIGN.md，使用连接真实 main process 的 disposable Browser QA；不把隔离数据验证写成真实账号验收。

## 验收方法

每项按源码与真实页面复现、修复、针对性检查、Browser 复验推进。关闭一个缺陷只代表该缺陷的证据闭环，不代表所属页面的所有状态已通过。真实账号、原生文件选择器、实际 capture 和运行中对话的验收独立记录。

## 已修复及已获得的证据

| 项目 | 修复 | Browser 证据与边界 |
| --- | --- | --- |
| Agent 模型菜单 | 空候选展示原因与配置说明，消除大空框；局部 Escape 与焦点返回；列内方向键 | 空配置真实 main 返回零候选，生产构建空态可见，Escape 保留 Settings；有凭据的候选列表未验证，不能断言截图中候选缺失的全部原因已解决 |
| Composer 模型按钮 | 未配置时显示“选择模型”，避免无名称空按钮 | 隔离项目入口实测按钮有名称，菜单空态与关闭可用 |
| Agent 图标与复制 | 图标菜单外点/Escape/方向键；重复复制使用独立 ID 与可区分名称 | 更换图标、保存、End 导航、Escape、重复复制完成；交接执行未验证 |
| Tools 表单 | Shell 与解释器统一控件样式，解释器字段响应式布局；保留未完成环境变量和参数输入；表单校验与保存状态 | 逐键输入环境变量与参数、45000 超时保存，切页重读一致；非法环境变量与清空超时阻止提交；生产构建截图确认尺寸与间距 |
| Scoped resources | 新建 ID 自动避重，手填冲突显式拒绝；MCP 参数拒绝非法 JSON/非字符串数组；列表移除空图标列 | Skills 新建保存、第二个新建 ID、冲突与取消；MCP 参数 [1] 被拒绝且输入保留；真实连接和 trust/revoke 未验证 |
| 资源空态 | 独立资源页使用合理列表/编辑区高度；Tools 中空 MCP 区保持紧凑，避免遮蔽下面的本机配置 | 桌面与窄屏页面审查；不将空态等同于长列表验收 |
| Provider 搜索 | 按供应商、模型 ID/名称筛选并提供无结果状态 | openai 筛到匹配项，无结果查询有明确提示；OpenAI 连接页凭据缺失时按钮禁用；未发真实认证或模型请求 |
| Settings 导航 | Shell/解释器搜索定位准确，窄屏导航增加左右方向键；颜色输入补可访问名称 | 搜索 python 跳转并高亮；九个设置页均实际访问；深浅主题切换与刷新持久化已检查 |
| 窄屏导航 | 左侧栏收起后仍可打开抽屉，项目、知识、用户设置可达；左右抽屉共用焦点与关闭逻辑；布局监听延至加载页结束后挂载 | 390px 打开左导航、长中文项目名称、Tab 回环、用户设置进入 Settings；最终生产构建 1564→390→1564 不刷新连续缩放，抽屉/停靠自动切换，Escape 还焦点，两个宽度水平溢出均为 0 |
| 用量弹窗 | 根据实际顶栏边界限制高度 | 修复前 390px 顶部为负数且标题/关闭被裁剪；修复后顶部 56px，高于 48px 顶栏下边界，无水平越界 |
| Capture 项目入口 | 中文界面文案补齐 | 隔离项目空态、刷新和入口可见；实际 .rdc 导入/预览未验证 |
| browser-dev | 仅精确 /app 文档映射到根路径，保留 /app/App.tsx；CSS HMR 使用 constructable stylesheet | 原白屏修复后真实页面可交互且样式完整；保持严格 CSP；生产构建入口也已真实启动 |

## 仍未关闭的验收项

- **真实模型与会话**：尚无本轮授权使用真实配置，隔离 QA 没有已连接供应商。模型有效候选、选择后持久化、发送/停止/重写、运行中任务、审批、交接均不能记为通过。
- **Capture 与本机交互**：仓库没有可用 .rdc；当前 Browser 工具无法操作原生目录选择器。为了进入项目页，向本轮 disposable QA 的 main-owned registry 放入一个临时长中文路径项目；这不证明原生“添加项目”流程成功。
- **MCP / Hook / Policy 深层编辑**：连接、信任/撤销与实际执行未覆盖。Hook 表单当前以逐行方式解析 YAML，复杂引号/多行字段的往返保真需要独立修复与验收；空命令保存仍呈现较底层的错误。
- **Knowledge 与运行记录**：Knowledge 的卡片/候选/冲突导航与空态已点击，但实际候选晋升、写入审批未验证；运行记录完整筛选/搜索、Terminal 运行链路未完成矩阵。
- **响应式全矩阵**：已有桌面、390px、深浅主题及若干键盘证据；尚未覆盖所有页面在所有主题、字体比例、长列表、运行态及 reduced-motion 的组合，不能称为“完美级”。

## 自动检查

- 生产构建、TypeScript、ESLint 通过。
- 最后一次 UI 与代理定向测试 43 文件 / 196 用例通过；布局监听挂载时机随后修复，另通过生产构建、TypeScript、ESLint 与连续缩放 Browser 复验。
- 正常本机权限、--maxWorkers=2 的全量测试：330 文件通过、1 文件失败；2409 用例通过、1 用例失败。失败是 ShellTool 的 Windows 原生 OEM 输出解码用例；没有弱化断言或改动 Shell 解码实现。该全量结果早于最后的 browser-dev 修复，不能作为最终 diff 全绿证明。
- Knowledge 与 Investigation 门禁正常本机权限复跑通过（hits=0）。其余独立门禁包括 architecture、repository-hygiene、appearance、session-projection、right-rail、browser-capability、agent-runtime、tool-system、provider-system 等已通过。
- check:gates 仍在 fidelity 阻断：Knowledge 8 个旧 class 与 5 个 testid 基线不匹配。HEAD 检索确认至少旧 brand/lanes/view 标识本轮前已存在失配；本轮没有用虚假 DOM 或整体刷新基线掩盖问题。
- 未运行覆盖率与 ratchet，不声称全量全绿。

## 工作区与运行清理

初始工作区只有既有未跟踪 .cursor/，本轮不改动它，不创建分支，不提交或推送。本轮 Browser QA launcher 均已停止，进程命令行检查未发现残留 QA launcher / Vite / Electron，canonical instance.lock 不存在；隔离目录残留锁只允许其 owner 已退出。桌面启动权已交还；未启动 canonical 桌面读取真实账号，不将此检查写成真实账号桌面验收。

## 2026-09-22 计划三表面收敛

适用基线：`941b839f4508a24d3b440695914e983088eea7e0` 上本轮工作区修改；没有提交、推送或发布。截图正文仅作为视觉样本。

| 范围 | 状态 | 实现与验证边界 |
| --- | --- | --- |
| 文档预览卡 | 通过 | 有界 Markdown、标题去重、自然高度、独立阅读入口；移除分节折叠、内部 URI 页脚及专用样式/本地化键。预览无隐藏可聚焦控件。 |
| 紧凑决策区 | 待验证 | 实现与组件/异步隔离测试通过：全部 handoff 逐项显示，修订按需展开、聚焦、空值拒绝、失败保留和重复提交保护；实时 Provider → plan_artifact → 人工批准续跑尚未验证。 |
| 对齐阅读器 | 通过 | Workbench 提供真实外壳边界，稳定 PlanReaderHost 避免断点切换重挂载关闭；深浅主题实测模糊、全文读取、长内容/代码滚动、Escape/遮罩关闭、Tab 循环和回焦。 |
| 工程收口 | 通过 | 定向 5 文件 18 用例、全量 439 文件 3057 用例、coverage ratchet、typecheck、lint、build、check:gates 通过，详见 acceptance-ledger。 |
| Electron 缩放 | 阻塞 | 当前工具只能控制 Browser，不能操作原生 Electron；浏览器视口与字体尺度不作为 Electron zoom 证明。 |

Browser 使用正式 disposable one-time bootstrap 进入 `/app`。隔离本地 Provider 尝试被现有有效目录路由/工具能力约束挡住，没有修改能力或权限边界。后续使用隔离持久化计划记录，经过产品 history、plan.read 和 hash 校验进入真实阅读面板；记录来源并非模型生成，不能证明实时待审门。恢复后的无活跃请求会由主进程拒绝，未绕过恢复契约。没有修改 DOM 或注入 renderer demo。

验收覆盖与剩余条件见 `acceptance-ledger.md` 同日期条目；历史报告不回写为本轮通过。
