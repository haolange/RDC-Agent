# Workflow Docs

- `debugger-mainchain-demo.md`: Debugger 主链演示。
- `workflow-conformance-analysis.md`: 流程遵循性分析。
- Agentic Trace 验证入口以 `fixtures/traces/agentic-trace-basic.json`、`npm run test:browser-session` 和必要时的 `npm run test:shell-smoke` 为准。
